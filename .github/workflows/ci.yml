name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: health_mvp
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    # CI 전용 값 (실제 비밀값 아님). LLM 키가 없으므로 템플릿 설명으로 테스트한다.
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/health_mvp?schema=public
      SESSION_SECRET: ci-only-session-secret-not-used-in-production-000
      NEXT_TELEMETRY_DISABLED: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: DB 마이그레이션
        run: npx prisma migrate deploy

      - name: Lint · 포맷 · 타입 검사 · 단위 테스트
        run: |
          npm run lint
          npm run format:check
          npm run typecheck
          npm run test

      - name: DB 통합 테스트
        run: npm run test:db

      - name: 보안 점검 (의존성)
        run: npm audit --audit-level=high

      - name: 빌드
        run: npm run build

      - name: E2E 테스트 (Playwright)
        run: |
          npx playwright install --with-deps chromium
          npm run test:e2e

      - name: E2E 리포트
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
